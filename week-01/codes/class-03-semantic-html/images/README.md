# images/

Put your own photo here and name it `profile.jpg`.

`index.html` looks for `images/profile.jpg`. If the file is missing, the page
still works and the browser shows the `alt` text instead — which is exactly the
point of writing good `alt` text.

File names are **case sensitive**: `Profile.JPG` is not `profile.jpg`.
